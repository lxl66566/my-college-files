.c文件编码为utf-8，若出现部分乱码，请更换编码查看。

其中，queue.c为第一题代码，stack.c为第二题代码，bintree.c为第三题代码。

编译器：gcc.exe (MinGW-W64 x86_64-posix-seh, built by Brecht Sanders) 11.2.0


作业题目：

1.设从键盘输入一系列的字符a1、a2、......、an。设计一个算法实现这样的功能：

1）若ai为数字字符，ai进队；

2）若ai为小写字母时，将队首元素出队，

3）若ai为其他字符，表示输入结束。要求使用环形队列。


2.编写一算法实现回文判断。称正读与反读都相同的字符序列为“回文”序列。

(不建议单纯地采用C中的数组实现，用数据结构的线性表、栈、队列等知识实现。)


3.编写一个递归算法，在以二叉链表为存储结构的二叉排序树T上查找关键字为key的结点。

二叉排序树的定义为：

空或有一个根，根的左子树若非空，则左子树上的所有结点的关键字值均小于根结点的值。

根的右子树若非空，则右子树上的所有结点的关键字值均大于根结点的值。根结点的左右子树同样是二叉排序树。